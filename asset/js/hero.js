document.addEventListener("DOMContentLoaded", function() {
    setTimeout(function() {
        document.querySelector('.hero').style.opacity = '1'; // Show the hero section
    }, 1500); // 500 milliseconds delay (adjust as needed)
});